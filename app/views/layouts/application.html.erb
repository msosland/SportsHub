<!DOCTYPE html>
<html>
  <head>
    <title>SportsHub</title>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.4.9/angular.min.js"></script>
    <script src="http://cdnjs.cloudflare.com/ajax/libs/angular-ui-router/0.2.18/angular-ui-router.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body ng-app="sportsHub">
    <ui-view></ui-view>
    <script type="text/ng-template" id="/home.html">
      <div ng-repeat="article in articles">
        <a href="#/articles/{{$index}}">{{article.title}}</a>
      </div>
      <form ng-submit="addArticle()">
        <input type="text" ng-model="title"></input>
        <input type="text" ng-model="body"></input>
        <button type="submit">Publish</button>
      </form>
    </script>
    <script type="text/ng-template" id="/articles.html">
      {{article.title}}
      {{article.body}}
      <div ng-repeat="comment in article.comments">
        {{comment.content}}
        {{comment.author}}
      </div>
      <form ng-submit="addComment()">
        <input type="text" ng-model="content"></input>
        <button type="submit">Comment</button>
      </form>
  </body>
</html>
